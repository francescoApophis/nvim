

/nvim 
  init.lua (lazy, plugins and map_leader are require from here)
  
  lua/
    plugin/ (actual nvim plugins)


  plugin/ (things that need to be exectuted at startup
    keymaps.lua
    options.lua

  after/ or ftplugin (I could put the autocmd for html here, but I don't how it is sourced)

